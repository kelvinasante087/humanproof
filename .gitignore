# Secrets — never commit
.env
.env.local
.env*.local

# Dependencies
node_modules/

# Next.js
.next/
out/
build/

# Misc
.DS_Store
*.log
.vercel

# Next.js dev-generated agent guidance (re-created by `next dev`)
AGENTS.md
CLAUDE.md

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Convex — regenerated by `npx convex dev` / `convex deploy`; app references functions by name,
# not from _generated, so these need not be tracked.
convex/_generated/
convex/tsconfig.json

# Agent tooling scaffolding added by `npx convex dev` (not part of the app)
.agents/
.claude/
skills-lock.json
