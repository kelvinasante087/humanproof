# Secrets — never commit
.env
.env.local
.env*.local

# Dependencies
node_modules/

# Next.js
.next/
out/
build/

# Misc
.DS_Store
*.log
.vercel

# Next.js dev-generated agent guidance (re-created by `next dev`)
AGENTS.md
CLAUDE.md

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Convex API types are tracked for clean builds; generated agent guidance stays local.
convex/_generated/ai/
convex/tsconfig.json

# Agent tooling scaffolding added by `npx convex dev` (not part of the app)
.agents/
.claude/
skills-lock.json

# Playwright CLI scratch output (not part of the app)
.playwright-cli/

# Playwright scratch output (not part of the app)
output/
